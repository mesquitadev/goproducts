server:
  port: 5001
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/positivoinvestimentos
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      properties:
          format_sql: true
      ddl-auto: update
logging:
  level:
      com:
        mesquitadev: DEBUG